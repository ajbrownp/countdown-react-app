import React from 'react';
import ReactDOM from 'react-dom'; // controls coding to browser
import App from './App.jsx'; // importing the new App component we just created

ReactDOM.render(
  <App></App>, document.getElementById('root')
);
